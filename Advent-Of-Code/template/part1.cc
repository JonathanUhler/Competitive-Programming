#include <fstream>
#include <sstream>
#include <iostream>
#include <string>


int main() {
	std::string line;
	std::ifstream infile("input.txt");
	while (std::getline(infile, line)) {
		
	}
	return 0;
}
